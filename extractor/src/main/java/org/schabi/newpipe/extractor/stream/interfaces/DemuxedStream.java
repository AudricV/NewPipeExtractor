package org.schabi.newpipe.extractor.stream.interfaces;

import org.schabi.newpipe.extractor.stream.interfaces.base.BaseDemuxedStream;

/**
 * A {@link Stream} containing only one type of data.
 *
 * <p>
 * See {@link BaseDemuxedStream} and {@link Stream} for available methods.
 * </p>
 */
public interface DemuxedStream extends BaseDemuxedStream, Stream {
}
